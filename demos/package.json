{
  "name": "demos",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn tsc && yarn cp-usr",
    "checksum": ". ../scripts/check.sh .",
    "demo": "yarn checksum ; cd dist ; node --enable-source-maps --",
    "clean": "rm -rf dist/",
    "build": "yarn clean && yarn tsc"
  },
  "dependencies": {
    "wretched": "file:../dist"
  },
  "devDependencies": {
    "typescript": "^5.4.3"
  }
}
